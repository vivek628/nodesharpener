'use strict';

const fs = require('./fs.js');
const NodeStorage = require('./NodeStorage.js');
const readJSONDb = fs.readJSONDb;

module.exports = absolutePathToDb => {
  var json = readJSONDb(absolutePathToDb);

  return new NodeStorage(absolutePathToDb, json);
};
